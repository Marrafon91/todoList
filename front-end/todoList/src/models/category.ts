export type CategoryDTO = {
  id: number;
  name: string;
  color: string;
  quantity: number;
};

export type CategorySummaryDTO = {
  id: number;
  name: string;
  color: string;
  quantity: number;
}
